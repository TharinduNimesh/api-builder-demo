# Example environment variables for docker-compose
# Copy to .env and fill real values before running

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# DATABASE_URL used by backend. Use service name "db" (docker-compose network).
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/api_builder_db

# Frontend base URL pointing to backend API
VITE_API_BASE_URL=http://localhost:3000